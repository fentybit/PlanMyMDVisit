<h4>Doctors available in our healthcare provider:</h4>

<% @doctors.order(specialty: :asc).each do |doctor| %>
    <div>
        <strong><%= link_to "#{ doctor.fullname }", doctor_path(doctor) %></strong>
        <i><%= doctor.specialty %></i>
    </div>
<% end %>